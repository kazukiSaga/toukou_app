<%= form_with(model: tweet2) do |form| %>
  <% if tweet2.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tweet2.errors.count, "error") %> prohibited this tweet2 from being saved:</h2>

      <ul>
        <% tweet2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
